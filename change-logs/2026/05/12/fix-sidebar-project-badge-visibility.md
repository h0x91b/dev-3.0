Made the project name badge in the Active Tasks sidebar much more visible in global scope. The badge is now larger, uses the accent color with a subtle background and border, and is rendered as the first line of each cross-project task card so the project is the first thing the user reads when scanning tasks from all projects. Also fixed horizontal alignment inside the sidebar: the search input now uses the same 12px horizontal padding as the rest of the panel, and the active-task indicator is rendered as an absolute 2px stripe instead of a `border-l-2` so card content stays symmetrically inset on both sides and aligned with the status group headers and separators.
