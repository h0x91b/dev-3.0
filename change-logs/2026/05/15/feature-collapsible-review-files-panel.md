Added a collapse toggle to the inline diff review's files panel — on small screens the files sidebar was leaving very little room for the diff (especially in side-by-side mode), and the column couldn't be made smaller. A chevron in the panel header now hides it, replacing it with a thin vertical strip that brings it back. The collapsed/expanded preference is persisted across sessions in localStorage.
