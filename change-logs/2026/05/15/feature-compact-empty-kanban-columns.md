Empty Kanban columns shrink to a slim fixed strip (~35% of the standard width) on narrow viewports (<1400px), with the title truncated to fit. Hover dwell of 300ms expands the column back to full width; drag-and-drop never triggers the expansion so the board stays stable during a drag. Todo stays full width because of the always-visible "+ New Task" button.
