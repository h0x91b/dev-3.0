Rewrote the README install section. Split macOS desktop app from Linux server (headless), and added a full step-by-step `dev3 remote` setup for Debian/Ubuntu — system deps, swap on 2 GB VMs, Bun, source build (using `bun --bun vite` to avoid Node OOMs when both runtimes are installed), and connection options (SSH port-forward, public IP, `--tunnel`). Plus optional `gh` install and a sample systemd unit. Validated end-to-end on a fresh Hetzner CPX11 (Ubuntu 24.04) — UI loads in the browser at the printed URL.
