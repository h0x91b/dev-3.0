Rewrote the README install section. Split macOS desktop app from Linux server (headless), made `brew install h0x91b/dev3/dev3` the primary path on Linux (and macOS for the CLI/headless variant) with a pre-built tarball as the no-brew fallback. Build-from-source moved to a "contributors" section at the bottom.

Wired up the Linux brew formula: added `scripts/create-cli-tarball.sh` that packs `dev3` + `dev3-server` + UI assets into `dev3-cli-{macos,linux}-{arm64,x64}.tar.gz`, called from each platform's `release.yml` build job; the CLI tarballs land in S3 (so they live next to the existing DMG/zst artifacts) and are uploaded to the GitHub Release; a new "Update Homebrew Formula" step in the `release` job rewrites `Formula/dev3.rb` in the `homebrew-dev3` tap with the right `version` + per-platform `sha256`. Cask flow is untouched.

Made the headless binary self-contained: `headless-entry.ts` now also probes `dirname(realpath(process.execPath))/dist` and `<bin>/../share/dev-3.0/dist` before falling back to the dev/CWD candidates, so an extracted tarball or a brew install just works without `DEV3_VIEWS_DIR`.

Fixed a remote-mode bug in `getProjectPtyUrl`: clicking Project Terminal failed with "Failed to connect to PTY server" because the override was missing in `src/mainview/rpc.ts` browserOverrides — the raw `ws://localhost:<ptyPort>` URL was sent to the laptop browser. Added the override so the URL routes through the `/pty?session=project-<id>&token=<jwt>` proxy like every other PTY URL in browser mode.

Validated end-to-end on a fresh Hetzner CPX11 (Ubuntu 24.04, dual-stack): `git clone` + build + `./dist/dev3 remote` gets the UI loading, Project Terminal works after the fix.

Restructured the Linux install section so it isn't framed as "headless only" anymore. Promoted the Homebrew install instructions out of the collapsed `<details>` block, added a visible warning that the Linux Homebrew installer refuses to run as `root` (with a one-liner to create a regular user), and added a "Desktop GUI on Linux" subsection pointing to the Electrobun `tar.zst` bundle for Linux machines with a display. The brew CLI install is now presented as the universal Linux entry point — works for headless servers and as CLI tooling on a Linux desktop.
