Deduplicate merge-completion prompts by persisting the last prompted task-branch fingerprint. Dismissing the prompt now suppresses repeats until the task branch changes, with a one-hour fallback when the exact branch head cannot be read. Fixed a race condition where two concurrent reservation calls could both pass the in-memory guard before either added the key; fixed polling loop that kept calling prepareMergeCompletionPrompt every 15 s after suppression.
