Strengthened the dev3 agent skill prompt so the session-start chores (branch rename, title, overview, labels) are no longer silently dropped when the user hands the agent a concrete task immediately. Added a front-loaded "Session-start checklist" anchored to an event ("before you end your first turn") instead of the easily-skipped "at session start", coupled title-setting to the initial-overview moment, and reworded the overview "keep current" rule to trigger on material state changes rather than a per-message count.
