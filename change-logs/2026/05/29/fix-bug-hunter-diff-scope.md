Fix the bug-hunter diff scope so it only looks at this branch's own changes. The hunter prompt now pins the fork point via `git merge-base <ref> HEAD` and diffs against that, instead of relying on a bare three-dot compare; it also honors the project's configured compareRef (and `local` mode) instead of always assuming `origin/<base>`. Prevents un-rebased branches from pulling unrelated files changed only on origin into the hunt.
