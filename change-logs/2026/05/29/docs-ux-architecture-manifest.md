Created the initial Product UX Bible under docs/ux/ — a repository-derived UX architecture manifest covering the object model, screen-based navigation, surface model, action taxonomy, design-token policy, complexity budgets, placement rules, and known anti-patterns. Includes human-readable bible, machine-readable ux-architecture.yaml, decisions, audit report, and glossary. Also wired AGENTS.md to point at the manifest and mandate the /ux-principal skill before any UI/UX work.

Then defined and applied a 2×2 quickbar model for the TaskInfoPanel inspector: two rows split left|right into four domain bars (Context, Session/Agent, Git, Runtime), one action domain per bar, with panel chrome pinned separately. Moved dev-server, scripts, and open-in out of the overloaded row-1-right cluster into the previously unused row-2-right (Runtime & access bar) beside Git, balancing it at three controls, and truncated the Context label strip to 4 inline chips plus a +k overflow chip. Tightened the inspector header spacing: grouped the Runtime cluster with a wider gap and gave the collapsed two-row layout vertical breathing room.

Added a prominent prevent-sleep ("Awake") toggle to the global header next to Home Terminal, backed by a new amber `--awake` design token in both themes. It keeps the machine awake while dev-3.0 is open (default on) and is forced on and locked while remote access is active. Surfaced the previously buried `preventSleepWhileRunning` setting and changed its semantics from "while agents run" to "while the app runs".
