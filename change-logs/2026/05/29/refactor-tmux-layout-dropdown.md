Reworked the tmux pane controls toolbar: collapsed the five always-visible layout preset buttons into a single "tmux layout" split-button (one-click cycle + a dropdown menu listing each preset with its icon, label and keyboard shortcut, highlighting the last applied one). The dropdown opens on hover, reuses the original cycle-layouts icon, and includes a "Cycle layouts" entry alongside the presets. Kept split/zoom/info/close inline, added aria-labels on every control and a divider before the destructive close-pane button. Also shortened the spawn-agent button label to a leading "+" to save toolbar space, gave the spawn-agent and Create PR buttons the robot_love_outline glyph, and compacted the diff include-tests toggle to a "with"/"without" word plus a single test-tube glyph (state read from the word, consistent across the info panel and diff toolbar), reusing a crossed-out test-tube glyph only on the summary badge to mark hidden tests.
