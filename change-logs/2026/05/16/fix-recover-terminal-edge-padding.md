Recover terminal edges that were reserved by stale workarounds. Drop the global `pb-7` (and matching startup window-resize nudge) — both were 1.14.4-era WKWebView clipping compensations that Electrobun 1.18.1 no longer needs. Also patch ghostty-web's FitAddon to remove its 15px native-scrollbar reservation, since ghostty already overlays its own scrollbar on the canvas. Net: ~1 row of vertical space and ~2 columns of horizontal space restored across all terminal views.
