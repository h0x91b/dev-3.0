Taught dev3 agents to use the surrounding `tmux -L dev3` session proactively. The main `/dev3` skill now carries a short summary — discovery commands, when to open a pane, and the most common pitfalls — and points to a new dedicated `/dev3-tmux` skill for the full command reference (split / new-window / send-keys / rename / swap / move / resize / capture / kill, plus pitfalls). The new skill is installed for Claude, Codex, Cursor, OpenCode, Gemini, and the generic `.agents/` directory, with matching OpenAI metadata.
