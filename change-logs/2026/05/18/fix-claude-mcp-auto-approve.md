Auto-approve project-scoped MCP servers (`.mcp.json`) in dev-3.0 managed Claude Code worktrees so users no longer face the "N new MCP servers found" prompt on every launch. Mirrors the existing trust-dialog bypass: when ensuring Claude trust for a worktree we now also seed `<worktree>/.claude/settings.local.json` with `enableAllProjectMcpServers: true`, merging any explicit approvals/rejections already present in the project's `.claude/settings*.json`.
