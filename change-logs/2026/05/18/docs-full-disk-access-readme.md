Expanded the macOS Full Disk Access troubleshooting section in README with the real symptom (git remote operations — fetch/pull/push/clone — hang indefinitely while local-only commands keep working) and a screenshot of the System Settings panel. Added a matching in-app stuck-preparation modal: when a task spends more than 3 minutes on the `Fetching origin` stage, dev-3.0 now pops a centered dialog with two action buttons — open the README troubleshooting anchor and, on macOS, deep-link to the Full Disk Access System Settings pane. New optional `Task.preparingStartedAt`, new RPC `openSystemSettings`, threshold exported as `STUCK_PREPARATION_FETCH_THRESHOLD_MS`. Decision: decisions/051-stuck-preparation-popup.md.
