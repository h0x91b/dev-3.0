Settings → Coding Agents now lets you reorder agents and configurations: each row has a drag handle plus ↑/↓ buttons so you can group items however you like (e.g. keep all Plan Mode configs next to each other in the dropdowns). The settings panel itself was widened (max-w-2xl → max-w-4xl) so it no longer looks cramped on large displays.

Suggested by @genrym (h0x91b/dev-3.0#480)
