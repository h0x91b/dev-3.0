The red "Close pane" button in the task tmux controls now only prompts for confirmation when you are about to close the *last* remaining pane — closing intermediate panes still happens instantly. On confirm, the kill is forced through so the user can actually take down the session if that is what they want. Reuses the existing `showConfirm` modal and last-pane refusal added in #573.
