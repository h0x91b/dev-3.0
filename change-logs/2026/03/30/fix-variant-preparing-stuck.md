Fixed a background preparation failure path where `spawnVariants` and `addAttempts` could reject before entering per-task error handling, leaving tasks stuck in `Preparing...`. Added regression coverage to ensure failed project config resolution clears the preparing state instead of silently hanging the card. Also added extra RPC logs around background preparation startup, config resolution, per-task preparation, and cleanup so the next incident leaves a usable trail instead of silent bullshit.
