Reworked the built-in Codex presets around the actual dev workflow: a planner profile plus GPT-5.4 and GPT-5.3 Codex medium/high variants, all with `--search` enabled and `--no-alt-screen` for tmux-friendly inline output. Also fixed Codex command building so resume sessions no longer receive unsupported generic flags like `--permission-mode`, `--effort`, or `--max-budget-usd`, and the settings preview now matches the real command.
