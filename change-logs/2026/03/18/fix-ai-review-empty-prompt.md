Fixed AI review agent launching without a prompt when builtinColumnAgents config has empty prompt field. The normalizeReviewPrompt function intentionally saves empty string for default prompts, but triggerColumnAgentIfNeeded was not falling back to DEFAULT_REVIEW_PROMPT when the config object existed with an empty prompt. Now each field (agentId, configId, prompt) is individually defaulted.
