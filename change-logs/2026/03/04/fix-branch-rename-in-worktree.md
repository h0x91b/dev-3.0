Fixed git operations (merge, unpushed count, worktree cleanup) breaking after branch rename in a worktree. Added `getCurrentBranch()` helper that reads the live branch name from git instead of relying on the stored `task.branchName`. The `getBranchStatus` handler now auto-syncs the stored branch name when it detects a rename. Added comprehensive unit tests covering branch rename scenarios for all git functions.
