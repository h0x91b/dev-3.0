When clicking a kanban card removes focus from the terminal, typing printable characters now automatically refocuses the terminal and forwards the keystroke. The fix attaches a document-level keydown listener in TerminalView that activates only when nothing meaningful has focus (activeElement === body) and a plain printable key is pressed (no Ctrl/Alt/Meta modifiers). Includes 8 new unit tests covering the happy path and all guard conditions.
