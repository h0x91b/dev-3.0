Optimized test suite from ~65s to ~6s for local development. Split monolithic git.test.ts into 4 parallel files with template repo optimization. Replaced real git with mocked spawn for thin wrapper tests. Both vitest suites (mainview + bun) now run in parallel via concurrently. Three heaviest files (git-worktree, git-merge-detection, shift-keys-e2e — ~80s combined) are excluded from `bun run test` and available via `bun run test:full` for CI/PR. Eliminated all console noise and React `act()` warnings from test output.
