Split monolithic git.test.ts (58s) into 4 parallel test files and introduced a template repo optimization (cp -r instead of full git init per test). Bun test suite wall time reduced from ~58s to ~23s, total `bun run test` from ~65s to ~37s.
