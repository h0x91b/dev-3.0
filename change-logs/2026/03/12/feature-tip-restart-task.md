Added a "Did you know?" tip explaining that users can restart a task from scratch by moving it to Cancelled and then back to Todo, which spawns a fresh worktree and agent session. Also fixed lint script to auto-generate changelog-bundled.ts in fresh worktrees (same pattern as build-info.generated.ts).

Suggested by @hagamitu-wix (h0x91b/dev-3.0#276)
