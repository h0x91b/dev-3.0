When an AI agent exits (e.g. via /exit), the terminal now drops into a live shell in the worktree instead of showing the "Terminal session ended" dialog. Users can run commands, switch agents, or type exit to finish. The dialog only appears when the shell itself exits.

Suggested by @AboMokh-Wix (h0x91b/dev-3.0#233)
