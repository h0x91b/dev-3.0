Rewrite Codex sandbox integration with dedicated `[permissions.dev3]` and `[profiles.dev3]` profiles using absolute paths. Fix CLI context detection when sandbox rewrites HOME=/tmp. Inject SHELL=/bin/bash to avoid seatbelt blocking homebrew zsh. Restructure Codex presets: non-bypass use `-a on-request` (not `--full-auto` which bypasses permission profiles), bypass presets keep `--sandbox danger-full-access`.
