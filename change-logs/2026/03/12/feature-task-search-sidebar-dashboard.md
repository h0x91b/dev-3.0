Add search/filter capability to the task sidebar (left panel) and the Kanban board. The sidebar now has a compact search input that filters active tasks by name, description, #ID, or UUID. Board search now also matches PR numbers (e.g., "PR123" or "#456") via the taskPrMap data.

Suggested by @sapirch (h0x91b/dev-3.0#284)
