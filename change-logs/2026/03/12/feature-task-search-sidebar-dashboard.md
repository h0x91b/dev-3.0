Add search/filter capability to the task sidebar (left panel) and the dashboard activity view. Search works across task name, description, #ID, UUID, and PR number. The existing board search also now supports PR number queries (e.g., "PR123" or "#456").

Suggested by @sapirch (h0x91b/dev-3.0#284)
