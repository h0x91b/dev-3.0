Renamed "In Progress" column to "Agent is Working" and "User Questions" to "Waiting for You" across all three locales (en, ru, es). Updated STATUS_LABELS for CLI output and fixed all affected tests.

Also annotated the skill with UI display names so agents can map user instructions like "move to agent is working" to the correct status identifier.
