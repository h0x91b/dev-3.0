Improved the dev3-project-config skill with mandatory port discovery and mapping instructions (step 3a). Agents now must research project port env vars and wire DEV3_PORT0..N into devScript before writing the config, and they are explicitly forbidden from guessing generic framework env vars when the repo does not prove the mapping.
