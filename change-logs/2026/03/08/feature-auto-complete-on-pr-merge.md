Background merge detection: the app now polls every 60 seconds and checks if tasks in "Review by You" status have their branch changes fully merged into the base branch. When detected (via squash, rebase, or regular merge), a native dialog offers to mark the task as completed. Uses pure git comparison (no `gh` CLI dependency). Dismissed notifications don't re-trigger until the task changes status.
