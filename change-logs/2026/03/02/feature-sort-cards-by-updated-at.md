Sort kanban cards by `lastActivityAt` — a new field stamped whenever a terminal bell fires (agent asking a question) or a PTY process exits. Cards with recent terminal activity bubble to the top/bottom of their column automatically. `movedAt` (status change time) is retained as a fallback when neither card has terminal activity.
