Refactored TaskInfoPanel header from a single crowded row into two rows to free up horizontal space. Top row now contains dev server, fullscreen, and tmux info (ⓘ icon only — hover reveals the shortcut popover). Bottom row contains the status dropdown and all git-related info (branch, ahead/behind, push/merge/rebase buttons, uncommitted changes). Collapsed panel height increased from 36px to 62px to accommodate the two rows.
