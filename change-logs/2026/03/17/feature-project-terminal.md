Add project-level terminal: a persistent shell session rooted at the project's git directory, accessible from the Kanban board toolbar and Dashboard project cards. Uses the same tmux + ghostty-web infrastructure as task terminals. One session per project, persists across navigation, reconnects seamlessly.

Suggested by @h0x91b (h0x91b/dev-3.0#335)
