Fixed notes/labels read-modify-write races by moving project and task mutations under locked mutator APIs in `src/bun/data.ts` and updating `src/bun/rpc-handlers/notes-labels.ts` to use them. Replaced JSON reads/writes in the data layer with `node:fs/promises` to avoid stale same-process reads that broke sequential locked updates, added regression coverage for concurrent project/task mutations, and added a critical `AGENTS.md` rule to keep Git commit/rebase messages inline so editors never pop up.
