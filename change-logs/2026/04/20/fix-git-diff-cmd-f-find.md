Inline Git Diff now opens its own local search with Cmd+F instead of falling through to board/task search or doing nothing. The search bar jumps between matching diff lines, uses a stronger warm highlight for matched text and the active row, keeps the board/sidebar shortcut disabled while diff is open, and now uses a cleaner compact toolbar layout that matches the surrounding diff header controls instead of duplicated metadata noise.
