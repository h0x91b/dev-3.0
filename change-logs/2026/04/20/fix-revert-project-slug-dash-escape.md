Revert the dash-escape projectSlug change and its one-shot data directory migration from PR #486. The migration silently renamed `~/.dev3.0/data/<slug>` on startup of v1.8.3, which broke v1.8.2 (still production) for the same project — every Kanban column showed empty because v1.8.2 still computed the old slug and looked in a now-renamed directory. The other four fixes from PR #486 (CLI `--description` tri-state, column-agent fallback, ProjectSettings worktree load race, activateTask note) are left untouched.
