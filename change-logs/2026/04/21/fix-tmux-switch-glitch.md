Fixed the garbled/overlapping terminal content that appeared when switching between tasks. The PTY server no longer runs a `capture-pane` replay on WebSocket reconnect — that replay used tmux's stale internal pane dimensions, which mismatched the freshly-sized client terminal. The client's resize dance now triggers tmux's natural SIGWINCH full-pane redraw, which paints cleanly with no competing flash.
