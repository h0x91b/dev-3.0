Added first-class Task Overview — a short one-paragraph summary surfaced above the terminal snapshot in the hover-preview popover, with inline editing and a new `dev3 overview set|show|clear` CLI. The dev3 agent skill now requires every task to have a clean overview so operators can re-enter focus fast on tasks they haven't touched in days; the original `description` is kept as a fallback when no overview is set yet.
