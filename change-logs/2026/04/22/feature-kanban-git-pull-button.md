Added a Git Pull button to the Kanban view toolbar. The button pulls origin into the project's main worktree with `git pull --ff-only origin <branch>` and is enabled only when that worktree is on `main` or `master`; otherwise it is disabled with a tooltip explaining why. The result (or any pull error) is shown as an alert so users no longer have to open the Project Terminal for a routine pull.
