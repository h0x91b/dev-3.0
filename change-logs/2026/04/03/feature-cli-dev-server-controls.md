Add `dev3 dev-server start|stop|restart|status` so the CLI can control the same task dev server flow as the UI button. The command now prints stable session, pane, PID, assigned `DEV3_PORT*`, detected listener ports, and worktree details after each action instead of guessing from unrelated listeners, prefers the newest live app socket when multiple dev3 app instances are running, and the generated `dev3` skill now treats dev-server start/stop/restart as explicit side-effectful actions.
