Added a project-scoped GitHub integration layer for `gh`: the app now detects installed/authenticated accounts, lets you choose a per-project GitHub account in Project Settings, and runs PR discovery/actions through that account instead of relying on whichever `gh` user happens to be active globally. PR polling, PR status lookup, merge detection, and PR open/create flows now all use the same backend wrapper.
