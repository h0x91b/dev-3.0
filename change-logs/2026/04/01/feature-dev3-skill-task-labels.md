Expanded the generated dev3 skill so agents assign 1-2 meaningful task labels during the same session-start metadata pass as branch renaming and task title cleanup. Tightened the wording so creating a label without immediately attaching it to the current task no longer counts as finishing the step, and added tests to keep the embedded label guidance consistent across Claude, Codex, and generic skill variants.
