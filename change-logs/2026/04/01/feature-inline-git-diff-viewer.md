Added an inline git diff mode that replaces the task terminal with an in-app diff viewer for branch, uncommitted, and unpushed changes. The viewer now keeps the unified/split switch, ESC/back navigation, and upstream/binary/large-file fallbacks, but renders files lazily for better responsiveness and no longer shows the whitespace-symbol toggle.
