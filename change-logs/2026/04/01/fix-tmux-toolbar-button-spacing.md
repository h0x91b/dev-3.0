Normalize the tmux toolbar spacing so the shortcuts hint button matches the surrounding controls and no longer leaves an extra gap before close pane.
Added a regression test that locks the shared button spacing classes in place.
