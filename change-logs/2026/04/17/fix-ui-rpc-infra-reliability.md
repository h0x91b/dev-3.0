Fixed five reliability regressions across the renderer, RPC transport, shell bootstrap, and release workflow. Failed app discovery no longer poisons the cache, merge-complete now surfaces double-failure instead of desyncing local state, browser RPC rejects in-flight calls on socket loss, release caches hash the real Bun lockfile, and unsupported shells are now guarded explicitly instead of pretending fish is zsh.
