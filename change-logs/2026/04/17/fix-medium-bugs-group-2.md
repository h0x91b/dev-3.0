Fix five medium bugs in task lifecycle and CLI: disambiguate projectSlug so paths like `/foo/bar-baz` and `/foo-bar/baz` no longer collide on disk (one-shot migration renames existing data dirs), make `task update --description ""` clear a task's description via tri-state parsing, surface a user-facing alert when a custom-column agent fails to launch (previously silent), prevent ProjectSettings from persisting a stale worktree config into a different worktree after getProjectConfigs fails (form is cleared and Save is disabled until load succeeds), and document the intentional description blanking in activateTask on reopen.
