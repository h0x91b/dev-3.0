Replaced the native folder dialog with a custom in-app folder picker so it works in both desktop and remote (browser) modes. The picker uses @headless-tree/react for lazy-loaded directory traversal over IPC and supports keyboard navigation, breadcrumbs, Home/root shortcuts, and pasting an absolute path. Affects "Add Project → Browse", "Clone base directory", and the global-settings clone-base-directory chooser.
