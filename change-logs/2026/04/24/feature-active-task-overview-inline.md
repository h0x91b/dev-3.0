The selected task in the Active Tasks sidebar now shows its overview directly inside its card (below the title, only when an overview is set), so you no longer need to hover to recall context. The pencil editor now saves your edits to a separate `userOverview` field that overrides the agent's summary everywhere — the agent keeps writing to its own `overview` field freely, but you see your version until you click the new "Revert to AI" (undo) icon next to the overview header.
