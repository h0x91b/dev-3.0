Add a "New" tab to Add Project that initialises a brand-new project from scratch: user picks (or creates) an empty folder in the custom picker and dev-3.0 runs git init, seeds a .dev3/README.md placeholder, and makes the first "init" commit automatically. The folder picker also gained a "New folder" button behind a new allowCreateFolder option so users can materialise a subfolder without leaving the app.
