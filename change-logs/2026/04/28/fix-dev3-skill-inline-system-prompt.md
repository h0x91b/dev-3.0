Inline the full dev3 skill body directly into the agent's system prompt instead of asking the agent to invoke `/dev3` first. The working directory is guaranteed to be inside a dev-3.0 managed worktree, so the conditional "MANDATORY: invoke skill" reminder was redundant and recent Claude Code versions started ignoring it. The skill rules (branch naming, status, overview refresh, scratch handling, etc.) are now always in context regardless of whether the agent loads the skill file. The `~/.claude/skills/dev3/SKILL.md` file remains for manual `/dev3` use.
