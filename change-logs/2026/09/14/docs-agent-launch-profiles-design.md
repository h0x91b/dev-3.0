Recorded the design for agent launch profiles: three named pointers at a launch preset — Microtask, Workhorse and Think Tank — that an agent names when it asks to start a task, while the user owns what each one maps to. The record covers the classification rubric, the seeded mappings per installed harness, the Workhorse-only fallback chain and the reworked launch-approval dialog; `docs/glossary.md` gains the term, kept distinct from model roles and recommended tiers. Design only, no behaviour change yet.
