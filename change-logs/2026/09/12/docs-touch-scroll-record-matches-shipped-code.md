The touch scroll-to-latest decision record now describes the code that shipped: the copy-mode poll arms only when a touch host passes `onScrolledIntoHistory`, reads the sessions `copyModeSessions()` resolves (a quick shell's own session, no dev-server sibling for a project key), and paste/submit leave copy-mode while `copyModePending()` holds. A stray JSDoc line above `PROJECT_SESSION_KEY_PREFIX` moved back to `computeTmuxSessionName`, and the per-pane scroll-signal test now also proves a background pane reporting "live" cannot clear the focused pane's key.
