Short: Find inside a file preview

⌘F (Ctrl+F) now opens a find bar inside the file preview opened from a terminal path click, searching both the raw listing and the rendered Markdown, with match counts, next/previous stepping and Escape to close. Previously the shortcut fell through to the board's own search behind the modal.
