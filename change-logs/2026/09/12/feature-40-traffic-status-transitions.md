Short: Traffic cards fade between statuses

A task card in Agent traffic no longer snaps to its new status: the status colour fades over 350ms across the whole card — wash, top rule, rail, dot, stamp and the zoomed-out cell fill — and the status word cross-fades over the one it replaces instead of being swapped under the cursor. With reduced motion on, the new status is simply there.
