Overlapping confirmation dialogs now queue instead of replacing each other. A second `confirm()` used to overwrite the first, so its dialog vanished mid-question and its promise never resolved — behind an agent-initiated completion or cancellation request that stranded the blocked CLI for its full ten-minute wait, and because a retry joins the pending request rather than pushing again, the dialog could never be drawn a second time.
