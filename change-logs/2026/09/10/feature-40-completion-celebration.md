Short: Confetti when a task completes

A task reaching Completed now gets a short confetti burst on its card in Agent traffic, with a badge naming the elapsed wall clock and where it is measured from — since work started, or since the task was created, and no number at all when the recorded movement log cannot prove either. The camera frames the card once, reduced motion gets the badge without particles, and opening the screen on already-finished tasks stays silent.
