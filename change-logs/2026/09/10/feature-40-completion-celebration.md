Short: Confetti when a task completes

A task reaching Completed now gets a short confetti burst on its card in Agent traffic, with a badge naming how long it took — measured from the recorded movement log, and labelled "completed in" only when a work start was actually recorded, otherwise as the task's age. The camera frames the card once, reduced motion gets the badge without particles, and opening the screen on already-finished tasks stays silent.
