Short: Sort tmux sessions by memory or CPU

The tmux Sessions popover now sorts by memory (default) or CPU, heaviest first, and the numbers go live while it is open — the resource monitor drops from a 10-second to a 2-second tick and pushes each session's CPU and memory as they move, then lapses back on its own once the list closes.
