Short: Artifacts open as a popup

HTML artifacts from `dev3 show-artifact` now open the same way images do — a popup over the task, closed with Escape or a click outside — instead of a panel docked beside the terminal. The drag-resizable artifact panel is gone, and with it the resize that could freeze the whole UI.
