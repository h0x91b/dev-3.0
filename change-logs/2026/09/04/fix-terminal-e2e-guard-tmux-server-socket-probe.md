The live terminal e2e gate now proves a leaked tmux server by connecting to its socket instead of relying on how tmux happens to name itself in the process table, and reports it with the socket name and the command that clears it. The guard's fixtures for a leaked server were hand-written strings the operating system never prints; they are replaced with output measured on macOS and Linux, and a decision record explains why a fixture of real command output has to come from a real run.
