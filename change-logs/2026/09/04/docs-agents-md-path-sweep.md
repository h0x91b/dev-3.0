Swept every file-path reference in AGENTS.md and docs/agents/ against the repo and fixed the ones that no longer resolved: the decision-record example now names a record that exists, the native menu bar is qualified as src/shared/application-menu.ts, and four keymap/i18n references that were written relative to src/mainview now carry their full path. A new test, src/bun/__tests__/documentation-paths.test.ts, asserts that every backticked file token and markdown link target in those docs still resolves, so the next one is caught before it misdirects an agent.
