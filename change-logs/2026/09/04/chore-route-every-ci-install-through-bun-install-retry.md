Every CI workflow now installs dependencies through the bounded-retry `bun install` action instead of calling `bun install` directly, so a transient registry tarball failure no longer kills a release run — release v1.51.2 lost all five platform builds to exactly that, in the one workflow the original retry never reached. A repo-wide test now fails if any workflow calls `bun install` directly or quietly drops `--frozen-lockfile`.
