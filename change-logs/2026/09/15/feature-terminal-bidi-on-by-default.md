Short: RTL terminal text on by default

Right-to-left reordering in terminal panes (BiDi) now ships on instead of being an opt-in beta toggle, so Hebrew, Arabic and Persian output reads correctly out of the box. The Settings → System → Advanced Experience switch stays, and turning it off is remembered.
