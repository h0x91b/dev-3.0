Short: Codex defaults to GPT-6 Astra again

The Codex default goes back to GPT-6 Astra Bypass at Medium effort, because some ChatGPT-account logins reject GPT-6 Sol with a 400. Installs the previous release moved onto Sol return to Astra on the default row only; GPT-6 Sol (now including its own Bypass Medium row) and GPT-6 Luna stay in the picker, and any explicitly chosen Sol or Luna preset keeps its model.
