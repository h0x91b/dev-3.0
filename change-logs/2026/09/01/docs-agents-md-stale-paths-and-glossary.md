Brought AGENTS.md back in line with the code: fixed three paths that no longer existed (the skill body now lives in `src/shared/agent-skill-content.ts`, tmux bindings in `src/bun/tmux/config.ts`, the plural-keys test under `src/mainview/i18n/`), dropped the stale nine-domain list of RPC handlers and the two conflicting decision-record counts, and replaced every bare "decision NNN" citation with its slug. The task-lifecycle and model-routing glossaries moved to `docs/glossary.md`, the TeamCreate section now says which harnesses actually have it, and MANDATORY is kept only on the sections whose violation silently ships a broken or leaking build.
