The agent instructions contradicted the verify-changes skill about the full test suite: the verification gate demanded `bun run test:full` locally before a PR, while the skill reserves that command for CI and forbids running it locally. The gate now matches the skill — the fast suite plus the suites around your change run locally, and the full run's verdict is read off CI for the head you pushed — and the fast run's exclusion count is corrected from six to the seven files it actually skips.
