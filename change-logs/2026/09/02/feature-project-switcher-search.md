Short: Search inside the project switcher

The header's project switcher now opens with a search field: type to fuzzy-filter the list by project name or by the name of a space it belongs to, walk the matches with ↑/↓ and open one with ⏎. The row beside it advertises the ⌘K "Go to Project" palette and opens it, so the same jump is one key away next time.
