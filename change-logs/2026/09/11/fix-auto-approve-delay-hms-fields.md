Short: Auto-approve delay in h/m/s

The auto-approve delay in Settings is now entered as three plain fields — hours, minutes and seconds — instead of an amount paired with a unit dropdown, so a value like 1h 30m no longer has to be converted to minutes by hand. The stored setting is unchanged, so an existing delay keeps working.
