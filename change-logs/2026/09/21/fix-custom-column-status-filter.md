Short: Status filter respects custom columns

The Status filter on the board and in the Active Tasks sidebar no longer shows tasks from an unchecked custom column just because their underlying built-in status is selected; a custom-column task now matches only its own column.
