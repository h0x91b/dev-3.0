GUI edits to task notes, task labels and project labels now reach other open windows, browser sessions and peer app instances immediately, and the GUI and `dev3` CLI share one implementation of the title, overview, completion-policy, note and label rules. Unknown label ids are now refused everywhere, unchanged edits no longer rewrite the board, and clearing a custom title restores the automatic title from the description on every surface.
