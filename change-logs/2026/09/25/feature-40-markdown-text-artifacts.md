Short: Publish Markdown or text artifacts

`dev3 show-artifact` now accepts a `.md` or `.txt` file and renders it into the same artifact viewer, themed like HTML reports, so a text-only report needs no hand-written HTML. Raw HTML inside the Markdown shows as text and unsafe links or local image paths fall back to their label.
