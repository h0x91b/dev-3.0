Short: Replay traffic on connected cards

Agent traffic defaults to Experiment 2 with named project blocks, five-card rows below each coordinator, message replay, speed/step controls and Follow/Focus navigation; wires avoid cards, and messages without a sender task still reveal their recipient. Follow frames each exchange once, returns to the whole scene when idle or replay ends, and starts with the last 24 hours; a calendar selects earlier local days and loads their retained messages automatically. Completed tasks stay marked and hibernated tasks remain grey; the inspector opens when needed, Experiment 1 remains available, and the separate Settings toggle controls the whole feature.
