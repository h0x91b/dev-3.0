Added Open Graph and Twitter card meta tags (og:image, twitter:card, og:url, canonical) to the landing page so shared links render a preview image, pointing at the dev3.h0x91b.com custom domain. Removed three screenshots that still carried hand-drawn green annotation marks left over from agent communication — file-browser and completed-column from the hero carousel and pr-review-mode from the README — and deleted the defective image files; the PR review feature stays described in text and the carousel rebuilds its dots from the remaining slides. Refreshed the hero carousel with new English-only screenshots: replaced the stale "Review by AI (unimplemented)" board shot and the dated activity dashboard, and added command-palette (⌘⇧P) and project-quick-switch (⌘K) slides; the README hero now uses the fresh board image too. Added three more English-only carousel slides for previously unshown killer features: the built-in inline diff/code-review viewer, the bug-hunters launcher, and the Option+Tab task switcher with live previews. Added a Remote Access (QR + Cloudflare tunnel) carousel slide, refreshed the global-settings (Coding Agents) and light-theme board shots, and expanded the README key-features list with remote/browser mode, built-in code review, bug hunters, and the command palette/quick-switch shortcuts. Replaced the stale Cyrillic multi-attempt shot with a fresh English board showing the siblings/variants popover. Replaced the stale terminal-view shot with a fresh English three-pane split running Claude Code, Codex and opencode side by side in one task worktree. Replaced the stale multi-agent-launch shot with a fresh English Spawn Agent dialog showing the agent picker (Claude, Codex, Gemini, opencode, Cursor). Replaced the static hover-preview PNG with an animated GIF (3x sped-up screen recording) of live per-second progress updating across kanban cards.
