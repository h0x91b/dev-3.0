Active Tasks sidebar now mirrors the kanban board: tasks parked in a custom column (e.g. "On hold") show under their own column group with the column's color instead of leaking into their underlying built-in status group, and built-in status groups exclude custom-column tasks. Within each group, tasks are sorted oldest-first by last status change (longest-waiting on top) so blocked tasks can't be buried.
