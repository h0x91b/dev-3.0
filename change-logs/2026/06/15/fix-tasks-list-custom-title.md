Fixed `dev3 tasks list` to display the custom/agent-set title via getTaskTitle() instead of the raw auto-generated title, matching `task show` and the app UI. The list is now ordered newest-first (descending seq), defaults to the newest 50 tasks, and supports `--offset` for paging with a "Showing X-Y of Z" footer.
