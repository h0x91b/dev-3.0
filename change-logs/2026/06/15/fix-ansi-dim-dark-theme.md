Fixed unreadable muted text in Claude Code's select prompts on the dark theme. Option descriptions, numbers and hints are drawn with the terminal dim attribute, which ghostty renders as 50% alpha — on the dark background that collapsed the default foreground into a low-contrast gray. The theme-readability filter now strips dim in the dark theme too (it already did in light), so that text renders at full intensity and stays legible.
