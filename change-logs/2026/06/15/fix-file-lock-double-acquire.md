Fixed a file-lock race where two processes could both acquire the same lock for a brand-new project: the ENOENT recovery branch used a recursive mkdir on the lock dir itself, which silently swallows EEXIST. It now creates only the parent directory and retries the atomic non-recursive mkdir, so cross-process mutual exclusion holds. The lock path and on-disk shape are unchanged, so older app versions stay interoperable.
