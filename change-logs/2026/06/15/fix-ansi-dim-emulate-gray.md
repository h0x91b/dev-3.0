Follow-up to the dim-readability fix: instead of dropping the terminal dim attribute entirely (which made the input ghost/placeholder suggestion render like already-typed text), dim over the default foreground is now emulated as an explicit muted gray. Muted text stays readable but clearly secondary in both themes, and the placeholder suggestion looks dimmed again. Colored dim still yields the full color.
