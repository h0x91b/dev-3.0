Fixed Claude agent-team teammates falling back to manual approval when the lead launched in an auto-approve mode (Auto, Bypass, Accept Edits, Don't Ask). The chosen config's permission mode is now written as permissions.defaultMode into the worktree's .claude/settings.local.json, so spawned teammates inherit the same auto-approve baseline instead of prompting "Waiting for tool approval" on every command.
