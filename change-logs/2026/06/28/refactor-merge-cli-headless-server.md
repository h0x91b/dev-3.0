Merged the standalone `dev3-server` headless binary into the `dev3` CLI: it's now a single binary, and `dev3 remote` boots the headless server in-process via a dynamic import instead of spawning a sibling. This drops the duplicated Bun runtime from releases and removes the sibling-locate/version-skew failure modes. A guard test keeps the heavy backend out of the CLI's startup path.
