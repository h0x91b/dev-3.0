Fixed the Launch Variants modal spawning agents when pressing Enter while the agent/config picker or the Watch/Cancel/Add/Remove buttons were keyboard-focused. The global Enter shortcut now only launches when no interactive control is focused. Also added a focus trap so Tab/Shift+Tab cycle within the dialog instead of escaping to the Kanban board behind it, with focus restored on close.
