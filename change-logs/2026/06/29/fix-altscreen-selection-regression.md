Fixed a regression where a double-click selection in the terminal stayed highlighted in place while the content scrolled or repainted underneath it (notably inside Claude Code). Runtime logs showed the offending TUI renders inline on the primary screen with mouse tracking on, so the alt-screen-only clear from the earlier fix never fired. The stale-selection clear now also triggers when the app owns the screen via mouse tracking, while plain shell scrollback selections stay anchored.
