Fixed labels selected in the New Task dialog being lost when the task was launched (Save & Start) — spawned variants and added attempts now inherit the source task's labels. Also added an inline "+ Add Label" control to the New Task dialog so labels can be created and assigned without leaving the form, and a hover hint on the Kanban label bar pointing users to Project Settings to rename, recolor or delete labels.
