Fixed a flaky CI test: the FolderPickerModal focus-trap test now polls for focus to land inside the dialog (useFocusTrap moves focus from a passive effect, so the old synchronous assertion raced under CI scheduling jitter). Also made the file logger self-healing — if its log directory is deleted after being cached, it now recreates the directory and retries once instead of spamming "Failed to write log file" ENOENT errors (the source of noisy stderr in the bun test suite).
