Added a narrow-viewport (mobile) tmux windows switcher: a slim bar above the panes carousel lets you list and move between tmux windows (separate workspaces in a session) with prev/next chevrons, a named dropdown, and Arrow keys. Backed by a new tmuxWindowNavigate RPC (list-windows + select-window) and pairs with the existing panes switcher so multi-window sessions are finally navigable on a phone over dev3 remote.
