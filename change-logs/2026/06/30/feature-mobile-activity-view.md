Made the Dashboard Activity view usable on a phone (sub-768px / dev3 remote): project cards go full-width, per-project actions and reorder collapse from the hover icon row into a touch kebab that opens a bottom-sheet (Open board, Settings, Finder, Terminal, Move up/down, Remove), and touch targets are bumped to ≥44px. Reorder, previously impossible on touch, is now reachable from the sheet. Task rows now stack on narrow viewports so titles stay readable instead of being squeezed by the status + time cluster; the project path is hidden, the kebab moves to the row end, each project caps at three attention rows behind a "show more" fold, "your turn" tasks sort first and get an accent strip.
