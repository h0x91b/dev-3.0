Planned the virtual "Operations" board: a non-git, cross-project Kanban for ad-hoc, code-driven tasks (back up prod, stop a server, mail/Slack triage, experiments) that persist in history, modeled as a new Project.kind: "virtual" with the git worktree excluded and a managed working directory hidden from the user. Documented the full feature plan, a UX decision record, and a concept.md status-tracker section, and resolved the three on-disk-invariant questions (virtual-project identity via a synthetic ~/.dev3.0/ops/<slug> path that leaves projectSlug frozen, managed working dirs under a new additive ops/ tree, and a separate virtual-projects.json so older app versions stay forward-compatible). No product code yet.
