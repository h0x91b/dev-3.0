Fixed the unreadable branch pill in Claude Code's status line on the dark theme. Claude emits the pill's pure-black foreground before its teal background; the theme-readability filter had already brightened the black to gray by the time the gating background arrived, leaving gray-on-cyan. The filter now remembers the original params of an adjusted foreground until text is drawn and re-emits them when a background that gates foreground adjustment opens first, restoring the intended black-on-teal contrast (and the light-theme mirror case).
