Added CI and PR-review status badges to task cards (CI passed/failed/running, review approved/changes-requested/commented), fetched by the existing background PR poller — which now keeps polling tasks in the PR Review column. A new signal raises the attention bell (respecting Focus Mode) and a native notification for watched tasks, surfaces the task in the attention sidebar until read, and clicking a badge moves the task back to your review.
