Auto-copy terminal selection to system clipboard. Two mechanisms: (1) tmux MouseDragEnd1Pane is bound to `copy-pipe-and-cancel "pbcopy"` for direct mouse-select-to-clipboard; (2) OSC 52 escape sequences from any app are intercepted server-side and piped to `pbcopy`. Client-side strips residual OSC 52 sequences before rendering.
