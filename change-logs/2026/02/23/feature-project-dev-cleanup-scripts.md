Added devScript and cleanupScript fields to Project settings alongside the existing setupScript. Both fields are stored in projects.json, exposed via the updateProjectSettings RPC, and rendered as textareas in the Project Settings UI. cleanupScript is intended to run when a task is moved to cancelled (or archived in the future); devScript is reserved for when dev server support is implemented. Documentation added to AGENTS.md.
