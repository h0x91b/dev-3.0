Fix Add Project not working: set browser-side RPC maxRequestTime to 120s (was 1s default — native dialogs and git ops timed out). Add structured logger with console output (colored, timestamped) and file logging to {userData}/logs/YYYY/MM/YYYY-MM-DD.log. Ensure parent directories exist before Bun.write(). Add detailed logging to all backend modules (rpc-handlers, data, git, pty-server, main).