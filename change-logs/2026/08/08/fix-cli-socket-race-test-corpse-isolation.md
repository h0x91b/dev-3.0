Fixed the load-dependent flake in the cli-socket label.delete and note race suites. A test that blew its time budget kept running, and because it read its project id, path and slug from mutable module scope it adopted the next test's identity and wrote its fixture and its injected concurrent write into that test's tasks.json. Each body now receives a frozen context created before its first await, injections are keyed by project, and the suites get a budget that a loaded machine cannot blow by accident.
