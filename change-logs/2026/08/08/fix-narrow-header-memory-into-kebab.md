Short: Memory readout folds into phone kebab

On narrow viewports the global header's memory-headroom readout no longer sits inline next to the breadcrumb — it folds into the kebab bottom sheet alongside prevent-sleep, rate limits, git pull and tmux sessions, and still opens its full breakdown there. Desktop widths are unchanged.
