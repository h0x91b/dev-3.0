Gave each test in the `label.delete` and `note.add`/`note.delete` race suites its own project, so a handler suspended on purpose by one test can no longer land its write in the next test's tasks.json. Each suite gains an isolation test that kills the neighbour deliberately and asserts the late write is unobservable, replacing a failure that only appeared under machine load.
