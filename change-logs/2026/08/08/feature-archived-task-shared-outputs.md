Short: Reopen a finished task's images and reports

Clicking a completed or cancelled task now lists every screenshot and HTML report the agent shared for it, each as its own link that opens the viewer at that item — no need to restart the task to see its own output. Escape now unwinds viewer first, then the task card, instead of closing the card underneath.
