Cached the dependency install in both Windows jobs of the packaged proof, where `bun install` measured 255s against 4-5s on Ubuntu and 10-36s on macOS, making it over half of the job that gates every release. Paying for it meant dropping the Linux node_modules cache, which measured a 7s restore against a 5s install while holding 4.8 GiB of the repository's 10 GB cache budget. The release graph is unchanged: the Windows proof still gates the build jobs, so a partial release stays impossible.
