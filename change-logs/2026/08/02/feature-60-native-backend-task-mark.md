Short: See which tasks run natively

Tasks whose terminal runs on the native backend now carry a small terminal glyph beside their title on the Kanban card, the Active Tasks sidebar and the task breadcrumb, with a hover tooltip and an accessible label. It states the stored backend only — not whether a terminal is currently running — and tmux tasks stay unmarked.
