Short: Mark a project as sensitive

Project Settings → Board now has a "Sensitive project" toggle. While streamer mode is on, such a project's name and tasks are blurred everywhere they appear, the project cannot be opened (the row shows a lock and a click explains why), and none of its notifications — desktop, in-app toast, attention badge, shared images or artifacts — are shown. With streamer mode off the project behaves exactly as before.
