Unix socket fixture paths in the test suites now come from one short, run-scoped socket root instead of the deep isolated temp dir, so they fit the kernel's 104-byte limit by construction rather than by luck. Four suites had answered this question four different ways, one of them 15 bytes over the limit with a comment claiming otherwise; an audit test now fails any test file that builds a socket path under a deep temp root.
