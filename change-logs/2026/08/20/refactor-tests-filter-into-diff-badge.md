The with/without-tests toggle is no longer a chip of its own in the task summary bar: it is now a nested icon segment inside the changed-files badge, boxed like the quick-complete segment of the status control, showing a struck-through flask while tests are excluded. Test files are excluded by default, and the badge keeps its raw totals when per-file stats are unavailable instead of collapsing to zero.
