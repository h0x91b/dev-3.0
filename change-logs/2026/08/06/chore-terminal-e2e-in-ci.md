The seven live terminal end-to-end scripts (real tmux server, real native host) now run in CI on both macOS and Ubuntu instead of only when someone remembered to run them locally. A new runner times every script, prints the runtime table on the run summary, and fails the job if any orphan process or throwaway temp dir survives; the gate hangs off the required `test` check, so a red terminal run blocks the merge.
