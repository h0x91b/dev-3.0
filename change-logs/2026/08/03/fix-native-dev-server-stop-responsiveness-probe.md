Added a renderer probe covering the native dev-server stop path: it drives the dev-server pane appearing, `stopDevServer` resolving and the pane set shrinking, then asserts the surviving pane is not remounted, `getPanePtyUrl` is not re-stormed, and ten start/stop cycles stay bounded. Written while diagnosing a full UI freeze after a native Stop Dev Server; the backend teardown itself was proven clean (539 ms, pane closed, 8 processes reaped, no stuck ports), so the invariant guards the renderer half of that path.
