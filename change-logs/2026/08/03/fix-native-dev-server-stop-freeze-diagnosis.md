Diagnosing the UI freeze after Stop Dev Server on a native task: the client-local pane focus now follows the pane set, so closing the focused dev-server pane hands focus to a surviving pane instead of leaving no pane focused and the viewer bar bound to a pane that is gone. Native viewer WebSocket connect/disconnect/attach lines now carry the pane id and session id, a stop carries one correlation id from request through aux-pane close to reply, and a terminal teardown that overruns its 50 ms budget says so. Adds a real-native-pane matrix that runs dev-server start/stop with one viewer and with two viewers attached to the same pane sessions.
