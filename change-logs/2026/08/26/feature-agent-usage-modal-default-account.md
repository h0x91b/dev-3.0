Short: Switch default account from usage popup

The header rate-limit pill now opens an Agent usage modal instead of a hover panel and a jump to Settings. It shows the same per-account quota cards for every Claude and Codex account, and each card is a radio row: click one to make it the default account for new launches, exactly as in Settings → Agent Accounts.
