Short: Switch default account from usage panel

The header rate-limit pill's panel now lets you switch the default agent account: it shows the same per-account quota cards for every Claude and Codex account, and each card is a radio row — click one to make it the default for new launches, exactly as in Settings → Agent Accounts. The panel still drops below the pill; a click pins it open so the rows can be used.
