The debug-ui QA skill now spells out the viewport rules for headless Chromium — set the viewport before opening the app and keep it at least 1024px wide — and adds a snapshot step that proves the page is actually clickable, not just screenshot-able. A new gotcha explains that an app which renders but refuses every click is the mobile portrait gate, with a one-line diagnostic to confirm it.
