The canary channel now publishes every 20 minutes instead of hourly while Windows is being shaken out on it, so a fix no longer waits up to an hour for a build to appear. The cadence is temporary and the workflow carries the original cron line verbatim for a one-line revert; a concurrency group was added at the same time so two publish runs can queue rather than race each other's manifests.
