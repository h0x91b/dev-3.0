Short: Edit the Review toggle's prompt

The prompt the Review toggle injects into a new task's description is now editable in Settings → Tasks & Board and, per project, in Project Settings → Project Config. A project override wins over the global one, which wins over the built-in localized text; both fields have a reset that falls back to what they inherit.
