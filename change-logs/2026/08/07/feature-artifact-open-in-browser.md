Short: Open an artifact in your browser

The artifact viewer header now has an external-link button that opens the artifact in your web browser, next to the download button. On the desktop app it hands the stored HTML file to your default browser; over remote access it opens the rendered document in a new tab.
