Investigated the intermittent "bravo journal holds only bravo's output" failure of the native-session lifecycle E2E: added a dispatch-only two-shell echo probe that reproduces cross-session output crossing with dev3 removed from the picture, and made the lifecycle fixture print the bytes surrounding a foreign marker so a failure names how it arrived. Every independent proof step in the packaged-runtime job now runs with `if: always()`, so one failure no longer hides the fourteen verdicts after it.
