Spaces now appear outside the changelog: the README gains a short "Spaces, when one flat list of projects stops working" subsection in the "What else is in there" block, and the landing page gains one feature card mid-grid. Both say the same three things — projects group into spaces, a project can sit in several at once, nothing moves on disk and every project keeps its own board — over two new screenshots shot against a throwaway demo state so nothing real is on screen.
