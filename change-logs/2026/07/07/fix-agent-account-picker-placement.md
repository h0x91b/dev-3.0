Fixed the agent account switcher in the task-launch picker: the account list now opens above the trigger (it previously spilled below over the Launch/Cancel buttons) and closes before the confirm dialog. The popover header now reads "Active account" with a note that the choice is global — machine-wide, applied to every agent dev3 launches, not per task — and the trigger shows the active account name with a switch caret.
