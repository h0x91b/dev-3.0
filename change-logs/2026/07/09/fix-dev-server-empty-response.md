Fixed `dev3 dev-server stop`/`restart` intermittently failing with "error: Empty response from server" during the tmux socket handoff. The CLI now treats an empty socket reply as a transient response-phase condition and briefly settles-and-retries the request for the idempotent dev-server operations, so a restart reliably brings the server back and stop reports success when the server is actually gone.
