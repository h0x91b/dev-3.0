Added a deterministic, test-only load and budget harness (new native-terminal-load-budget module, nested inside the native-session terminal registry) over the native-terminal parser and resync primitives, measuring bytes, frames, queue high-water marks, snapshot size, and drain iterations across 1-, 6-, and 20-stream scenarios. It exercises steady, burst, stalled-observer, sequence-gap-then-resync, and bounded-overflow loads and asserts the existing caps, explicit overflow behavior, and cleanup semantics without changing any production code.
