Add a pure terminal backend identity codec (MIG-003, tmux-removal roadmap): a missing field decodes to the effective legacy tmux while preserving that it was absent, explicit tmux/native round-trip deterministically, and unknown values, wrong types, or malformed containers fail with a typed result instead of silently selecting native. Isolated groundwork with no persistence, migration, or product wiring.
