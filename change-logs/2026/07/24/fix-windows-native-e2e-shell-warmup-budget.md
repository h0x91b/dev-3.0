Fixed a Windows-only flake in the native-session E2E suite: the probes that wait for a freshly started interactive shell allowed only ~4 seconds, so on a cold CI runner PowerShell answered just after they gave up and the lifecycle run failed. The warm-up budget now lives in one shared constant with room for a slow runner, and it is a ceiling rather than a delay — the POSIX runs still finish in seconds.
