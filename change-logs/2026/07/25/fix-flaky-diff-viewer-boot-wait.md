Fixed the flaky TaskDiffViewer test that failed on CI shard 5 whenever the runner stalled: component-boot waits now yield a bounded number of event-loop ticks instead of racing Testing Library's 1000ms wall-clock budget, so a slow machine can no longer fail a test whose DOM does settle. CI test runs also retry a failing test up to three times as a net for the remaining wall-clock-sensitive suites.
