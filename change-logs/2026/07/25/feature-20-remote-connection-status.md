Short: See when remote loses connection

Remote (browser) mode no longer reads as frozen when the link drops: a floating pill names the connection state, doubles as an instant retry, and confirms "Back online" once recovered. The Kanban board now shows a loading skeleton and a retry panel instead of a silently empty board, and reloads itself as soon as the connection is back.
