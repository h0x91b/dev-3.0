Added an internal, backend-neutral terminal-backend contract with two real adapters behind it — the existing tmux implementation and the merged native single-view lifecycle — so future work can target one product vocabulary instead of tmux specifics. tmux remains the only backend in use and all app, CLI, and terminal behavior is unchanged; the seam has no production callers yet and is covered by one conformance suite that runs against both adapters.
