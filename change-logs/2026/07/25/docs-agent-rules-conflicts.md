Cleaned up contradictions and dead weight in the agent instructions: the parallelism section no longer mandates a `TeamCreate` tool most harnesses do not expose (preference ladder capped at 5 members, falling back to `Workflow`/`Agent`), the response-style section states that Decision-First governs structure only while the user's language and tone always win, the two overlapping test gates became one escalating `Verification gates` block (nothing gates a commit; lint plus touched tests before push; full suite before opening a PR and after any rebase), the changelog rules merged the `Short:`/popover-priority guidance into a single entry, and coverage expectations now say plainly that no coverage provider is wired up so the percentages are review-time judgment rather than a machine check. Added a `No deprecation — ever` section, moved the git-editor and worktree read-only rules out to the personal global instructions, and dropped the obsolete Nerd Font icon section.
