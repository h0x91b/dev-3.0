The tmux dependency inventory is no longer a pull-request gate: `bun run test` now runs only the audit's pure unit tests, and the full live repository scan moved to one manual command (`bun src/cli/tmux-audit/generate.ts`, or `--check` to verify without writing) that is run at tmux-removal milestones. The scanner, classification model, generator, and generated inventory are unchanged.
