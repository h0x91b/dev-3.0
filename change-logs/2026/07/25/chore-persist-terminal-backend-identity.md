A task's primary terminal can now carry an explicitly selected terminal backend that survives a restart, stored as an optional field decoded through the shared identity codec. Tasks written before the field keep no field at all and continue to mean tmux — nothing backfills or rewrites them — and unknown stored values fail loudly instead of silently switching backends. Nothing selects a backend yet; this is persistence plumbing only.
