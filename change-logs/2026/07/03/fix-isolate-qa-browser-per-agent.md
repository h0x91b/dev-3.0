Isolated the browser-QA flow per task so parallel agents stop stomping each other. The debug-ui skill now exports AGENT_BROWSER_SESSION="dev3-${DEV3_TASK_ID%%-*}" before driving agent-browser, giving each task its own isolated browser session/profile instead of the shared global "default" one where one agent's navigation leaked into another's screenshot. Documented the singleton gotcha in the skill and AGENTS.md.
