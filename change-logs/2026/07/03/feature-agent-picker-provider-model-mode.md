Redesigned the Launch Task / add-variant agent picker from a flat 2-field control (Agent + one long Configuration dropdown — 21 presets for Claude) into a 3-field Provider → Model → Mode cascade, so you narrow from ~4 models to ≤7 modes instead of scanning one 21-row list. Purely a UI grouping over the existing presets — the flat configId stays the storage key, so nothing migrates and per-preset curation is preserved. Changing the model keeps your current mode kind when the new model has it.
