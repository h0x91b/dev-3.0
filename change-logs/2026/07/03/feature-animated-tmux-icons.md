Tmux toolbar icons now animate on hover, acting out the operation they trigger: splits saw their divider across, cycle-layout swaps the panes, zoom inflates the pane, close draws the X and blinks the pane out, layout presets draw their dividers, and the mobile New Window icon pops into existence. Shared glyph components live in TmuxIcons.tsx. The git action row (Show Diff, Rebase, Push, Create PR, PR + auto-merge, Merge) and the diff include-tests toggle now use matching hand-drawn animated icons (GitIcons.tsx): the diff types its added line and blinks the removed one, the rebase commit rides the rail to the tip, the push arrow flies into the cloud, the PR sparkle twinkles, the auto-merge bolt strikes, the merge commit lands, the tests flask boils. Git buttons are flex-centered and use compact labels (Diff, PR, Auto PR) so the row with icons stays no wider than before. The desktop tmux pane toolbar also gains a green New Window button (between the split buttons and the layout pill) that runs tmux new-window, with the same animated icon. The global header's Report and Changelog buttons are now icon-only (labels dropped, tooltips kept) to tighten the row. The entire top header row was then redesigned: the old mix of Nerd Font glyphs and ad-hoc inline SVGs (back, forward, home breadcrumb, project dropdown, no-sleep coffee/lock, quick shell, project terminal, pull with its refresh/success/alert states, remote QR, productivity stats, GitHub, report, changelog, overflow kebab, project-settings wrench, global-settings sliders, update-ready) is replaced by a single hand-drawn stroke set (HeaderIcons.tsx) matching the tmux and git icon style, each with a hover animation acting out its operation. The task header row was redesigned the same way: the watch toggle, bug-hunter and spawn-agent buttons, worktree settings, Open in…, file browser, scripts, ports, shared images, and the fullscreen / collapse panel chrome now use a single hand-drawn stroke set (TaskIcons.tsx), each with a hover animation acting out its operation — the watch bell rings with sound waves, the caught bug squirms under a focusing lens (so it stops looking identical to the header's report-a-bug glyph), the agent robot tilts hello and pings, the open-in arrow launches out of its box, the file and port trees wire themselves up, the scripts ƒ writes itself, the images sun rises at golden hour, the settings gear ratchets, the fullscreen corners fly, the panel chevron dips with a ghost echo.
