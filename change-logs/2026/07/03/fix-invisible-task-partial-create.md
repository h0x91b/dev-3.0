Fixed a bug in the Create Task modal where a task created successfully but failing to apply its custom title or labels would vanish from the board (invisible orphan) and let a retry create a duplicate. The new task now appears on the board immediately after creation; title/label application is a non-fatal follow-up that surfaces a warning without hiding the task or blocking the modal.
