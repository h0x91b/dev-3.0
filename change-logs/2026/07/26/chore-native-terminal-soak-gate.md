Added an opt-in multi-session soak and fault-recovery gate for the native terminal host (`bun run test:native-soak`, plus a manual GitHub workflow and a Windows PowerShell runner). It drives concurrent sessions through sustained agent-like TUI output, resizes, observer/writer takeover, forced reconnect churn, a cold controller reattach, a host crash with nested descendants, and repeated create/stop cycles, then emits a machine-readable verdict against budgets derived from the run's own saturated baseline. The normal test suite and required PR checks do not run it.
