Add an optional `--duration` flag to `dev3 notify` for short-lived in-app toasts from 2 to 30 seconds; the `s` suffix is optional, so `--duration 15` means 15 seconds. The existing 30-second default remains, and native desktop notifications are governed by the operating system. The compact command list keeps this optional flag in the detailed help only.
