Fixed the mobile task view never showing the diff summary badge ("N files +X −Y") in the summary bar: branch status was only fetched by the desktop git bar, which does not render on narrow viewports. The narrow TaskInfoPanel now owns a single branch-status instance that drives the badge, restores the merge-completion prompt on mobile, and pre-loads the git actions sheet so Rebase/Push/Create PR rows no longer flash "Loading branch status" on open. Also added a discovery tip pointing at the mobile ⋮ menu's Git section.
