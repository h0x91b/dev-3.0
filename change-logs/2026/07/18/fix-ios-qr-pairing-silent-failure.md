Fixed native iOS pairing where tapping "Pair" after scanning the desktop QR code did nothing: the one-time 30-second QR token is now exchanged the instant the code is scanned instead of after an open-ended "name this instance" step. Also fixed a follow-on bug where pairing to a desktop without the new /instance endpoint spun forever on "Opening dev3…" with no feedback — it now stops and shows a clear "update the desktop / scan again" error. Added an in-app Diagnostics screen (reachable from the pairing screen and Settings) that records a redacted trace of pairing/connection activity and can be shared via the iOS share sheet, so opaque failures are inspectable.
