Consolidated every tmux interaction behind a typed TmuxClient module (src/bun/tmux/): all 63 raw tmux spawn sites, 5 output parsers, 3 separator conventions and inline dev3-* session naming collapsed into one client with typed subcommand methods, declarative -F formats and a reverse session-name parser. The tmux-3.6 binary/shim selection moved inside the module verbatim, so bypassing it is no longer reachable by construction; the renderer↔pty resize protocol got a single shared encoder/parser. Pure refactor — terminals, splits, dev servers and alt-click behave exactly as before, now with unit and live-tmux e2e coverage.
