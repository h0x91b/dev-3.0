Rebuilt the Task actions bottom sheet on narrow (mobile) viewports. It was a dumping ground of dense desktop icon buttons; it is now a short, curated list of full-width touch rows. Desktop-only actions that make no sense on a phone were dropped from mobile (open-in editors/file browser, scripts, tmux layout controls, dev server, git mutations, worktree config, include-tests) — the diff stays reachable via the summary-bar badge. Kept: watch, spawn agent, bug hunters, exposed ports/tunnels (open the running app on the phone), shared images, and read-only details.
