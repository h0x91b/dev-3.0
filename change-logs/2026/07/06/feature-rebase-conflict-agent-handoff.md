The Rebase git button now handles conflicts instead of going dead. When the branch can rebase cleanly it still runs `git rebase` in a visible terminal pane as before; when it can't (conflicts), the button stays enabled, relabels to "Rebase (AI)", and hands the rebase off to the agent in the task terminal — the same mechanism as the Create-PR button — with a toast confirming the handoff.
