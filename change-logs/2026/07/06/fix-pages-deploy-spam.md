Replace GitHub's auto legacy "Deploy from a branch" Pages workflow with a custom Actions workflow that only runs when docs/ changes, serializes deploys via a concurrency group, retries the deploy once in-job to absorb transient "Deployment failed, try again later." backend errors, and never fails the run (emits a warning instead) — eliminating the false "Run failed" deployment emails.
