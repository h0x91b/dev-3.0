Pinned the packaged Electrobun runtime to Bun 1.3.14 on every platform and added a native Windows package proof that stages the bundled runtime under an updater-safe host name, re-enters it detached, reattaches to the same PIDs, and starts PowerShell through raw Bun.Terminal with no system Bun. The isolated tracer verifies clean shutdown and reports actionable runtime incompatibilities; production tmux behavior remains unchanged. Runtime guards accept Windows 8.3 aliases only for the same file and obtain PTY readiness from a quote-safe encoded PowerShell startup command instead of racing the first interactive prompt.
