iOS companion, build 6 feedback round: fixed the iPad share-sheet crash (popover anchor), the spurious "Terminal disconnected" flash while a terminal is still attaching, pinch-zoom clipping a terminal row (resize coalescing and debounce), and stale pane output after switching tmux panes. Pairing now distinguishes desktop-too-old from unreachable-or-errored with accurate guidance, probes candidates with a 5s timeout instead of 60s, asks for confirmation before removing a paired instance, and the desktop stops advertising VPN/VM-bridge addresses as the pairing host. Board columns gained previous/next chevrons, the terminal accessory bar gained a hide-keyboard button, and a persisted "Verbose logging" toggle in Settings records gesture-level diagnostics.
 Also fixed the launch/board watchdog crash (0x8BADF00D) on real devices: task sorting constructed an ICU-heavy date formatter per comparison on the main thread — and its default options silently failed to parse the desktop's fractional-second timestamps, so completed columns were never actually recency-sorted; both are fixed.
