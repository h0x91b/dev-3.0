Short: Remove remote active-task strip

Remove the persistent browser-only active-task strip so remote task workspaces keep the terminal full-width without the distracting horizontal scrollbar; existing task-switcher and breadcrumb navigation remain available.
