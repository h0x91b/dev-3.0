Terminal output now preserves the visible scrollback position while you read or copy older text, instead of snapping to the live bottom. Existing terminal selection invalidation behavior is unchanged.

Suggested by @DolevSol (h0x91b/dev-3.0#978)
