Fixed the mobile task view never showing the diff summary badge ("N files +X −Y") in the summary bar: branch status was only fetched by the desktop git bar, which does not render on narrow viewports. The narrow TaskInfoPanel now owns a single branch-status instance that drives the badge, restores the merge-completion prompt on mobile, and pre-loads the git actions sheet so Rebase/Push/Create PR rows no longer flash "Loading branch status" on open. Also reworked the cramped diff-viewer header on phones into structured rows — icon back button, readable title with inline ±stats, a wrapping mode/filter row, and a full-width search row — and added a discovery tip pointing at the mobile ⋮ menu's Git section.
