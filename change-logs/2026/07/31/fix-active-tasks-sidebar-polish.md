Short: Polished the Active Tasks sidebar

The Active Tasks sidebar got a detail pass: the scope toggles now cross-fade between outline and filled glyphs so the active scope reads as more than a colour change, every icon button has press feedback, the attention counter no longer crowds the panel-toggle icon, the bell badge uses the danger design token instead of a raw palette colour, and the pulsing bell plus the busy card rail honour "reduce motion".
