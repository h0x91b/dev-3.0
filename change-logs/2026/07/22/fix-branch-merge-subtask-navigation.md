The task shown in the branch-merged completion prompt is now clickable. Opening it postpones the prompt and navigates to that task. The dev3 agent protocol now keeps tasks open until work is safely preserved in the required destination — usually a pull request merged into main, but also an external file, task note, shared artifact, or another explicit destination — unless the user directly requests completion.
