Froze the isolated native-session registry's loopback transport as a deliberately small local protocol v1: a version-agnostic hello handshake that answers a mismatched client with one explicit version-mismatch error, request ids only on the hello and status request/response pairs, one compact error shape, and an oversized-frame guard — all leaving the host and shell alive. The existing per-session token check, tmux-backed production terminals, and persisted project/task data are unchanged; the module stays out of the production import graph.
