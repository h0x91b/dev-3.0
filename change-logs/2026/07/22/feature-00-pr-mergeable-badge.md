Short: PR mergeability badge on cards

Kanban task cards now show a PR mergeability badge (green "Mergeable" or a red reason such as Conflicts/Blocked/Behind) that replaces the old standalone CI badge; GitHub's merge-state already folds in failing and blocking checks, and the hover popover keeps the per-check breakdown.
