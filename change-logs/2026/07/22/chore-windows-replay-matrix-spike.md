Added runnable Windows shell/agent capture-replay tooling to the terminal-state spike (STATE-004): a scripted live-PTY capture, a static non-Ghostty terminal-query responder, an offline replay verifier, and a fail-closed sanitizer, driven by a single Windows runner for cmd.exe, PowerShell 7, Claude, and Codex. Evidence is sanitized (shells as fixtures when clean, agents as hashes plus metrics) and the work stays outside the production import graph.
