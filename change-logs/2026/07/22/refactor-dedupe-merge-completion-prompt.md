Extracted a single shared offerMergeCompletion primitive so the "Branch merged → complete the task?" outcome-card prompt has one implementation instead of three drifted copies (the App-level merge-detection push handler and the task info-panel branch-status hook's poll and post-merge paths). The primitive owns the dialog copy, the project/task context card, fingerprint reserve/dismiss bookkeeping, the notify/suppress gate, cross-client de-dup and the manual opt-out; each caller keeps only its own completion side effect. No visible change — the dialog looks and behaves exactly as before.
