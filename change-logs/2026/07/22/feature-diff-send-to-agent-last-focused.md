Short: Send review to last-focused agent

The diff review's Send to Agent now routes to the agent pane you last focused, tracked per tmux session, instead of whatever pane happens to be active — so with several agents it follows the one you were working in and no longer misfires into a focused shell or dev-server split. Hover tooltips on the Send and Copy buttons and the review help card now explain where each sends.
