The Active Tasks sidebar now shows each task's priority as the same colored P0–P4 badge used on the kanban card (click it to re-prioritize the whole variant group), and groups the queue into readiness tiers — Needs You, your custom columns, then Waiting — each sorted by priority band with the longest-waiting task on top. A compact status label on every card keeps the merged statuses legible, and the cross-project attention/bell scope adopts the same priority-first order.
