Recorded the design for an AgentAdapter interface (decision 124) that collapses the per-agent `is*Command` ladders in agents.ts, tmux-pty.ts, agent-hooks.ts, and agent-skills.ts into one pure descriptor per agent plus a bun-side executor, following the existing llm-provider.ts registry precedent. Design only — no code change yet; the agent support matrix now points at the planned interface.
