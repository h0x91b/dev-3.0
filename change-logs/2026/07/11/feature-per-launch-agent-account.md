Agent accounts can now be chosen per launch. Every spawn dialog (Launch Variants, Spawn Agent, Bug Hunters) gets an account selector so each session runs under the account you pick, instead of a single global active one; the former global switch becomes a default preselect. Claude isolates per session via CLAUDE_CONFIG_DIR and Codex via CODEX_HOME. The chosen account is remembered for Retry.
