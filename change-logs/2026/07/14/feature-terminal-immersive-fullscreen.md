Add an ephemeral task-terminal immersive fullscreen mode with a minimal dev3 exit strip, F11 and Cmd/Ctrl+Shift+F toggles, shortcut tooltip guidance, preserved tmux layouts, and notification navigation that exits fullscreen first. The focus chrome uses neutral compact surfaces, and the high-priority terminal tip explains that tmux state stays untouched. Agent-facing notifications and viewer events now queue across renderer, browser, CLI, and native delivery while immersive fullscreen or persistent Focus Mode is active, then flush after the final mode ends with task actions preserved.
