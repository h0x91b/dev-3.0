Addressed a batch of iOS companion-app TestFlight feedback. Compacted the terminal composer by dropping the redundant "Insert / Compose mode" row (the mode is already shown by the accessory-bar toggle) and moving Insert next to Send under a full-width message field, reclaiming vertical space. Moved the Task Info Actions section (Complete/Cancel/Delete) directly beneath Overview so it is reachable near the top without scrolling. Fixed the diff view rendering every line of a modified file as a deletion — modified files without server hunks now compute a real line-by-line diff on-device (common prefix/suffix trim plus an LCS), so unchanged lines stay context. Added a stale-while-revalidate diff cache shared across navigation so switching diff modes or reopening a task's changes shows the last result instantly and refreshes in the background. Kept the terminal composer short in landscape (compact height) so the terminal keeps usable real estate instead of collapsing behind the input. Reworded the notifications footnote to explain that notifications come directly from your Mac over a private live connection (no cloud), which is why they need that connection.
