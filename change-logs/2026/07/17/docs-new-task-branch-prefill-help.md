Added an inline help (i) icon next to the branch field in the New Task dialog that explains why a branch is prefilled from the project's currently checked-out branch (so small tasks stack onto a big feature) and how clearing the field falls back to the default base branch, and reworded the branch tip to match. Evolved the inline-help doctrine on top of that: help coverage is now owed (every surface and non-self-evident field must be reachable in help mode), every HelpSpot auto-registers as a help-mode zone so inline help always correlates with the (?) overlay, a new test guards dangling/orphan help ids, and the silently lost help topics for the Tasks & Board, Terminal, System, and Accounts settings categories plus the header rate-limit indicator were restored.

Suggested by @OrenZak (h0x91b/dev-3.0#979)
