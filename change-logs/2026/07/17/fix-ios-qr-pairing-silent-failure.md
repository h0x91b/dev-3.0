Fixed native iOS pairing where tapping "Pair" after scanning the desktop QR code did nothing. The one-time 30-second QR token is now exchanged the instant the code is scanned instead of after an open-ended "name this instance" step (by which point the token had expired), and a rejected or expired pairing now shows an actionable "scan a fresh QR code" error instead of silently sitting on the screen.
