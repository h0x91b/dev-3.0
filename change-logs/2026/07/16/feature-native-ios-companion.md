Scaffolded the native dev3 iOS companion with an accessible pairing and connected shell, isolated Swift packages, SwiftTerm, bundled Nerd Font assets, an opaque production app icon, a token-based launch screen, generated Xcode project configuration, tests, linting, and CI verification; added stable instance discovery and long-lived native sessions; froze the remote HTTP, RPC, push, and PTY wire contract in documentation and deterministic backend tests, including typed shared-media and task-removal dispatch; buffered bounded PTY input during proxy startup so the first resize and keystrokes are delivered in order; generated native semantic themes, status, label, and terminal palettes, and Nerd Font glyphs directly from the desktop design sources with drift checks; added a frame-coalesced native terminal with raw-input guards, desktop-compatible Shift keys, zoom persistence and cell fitting, selection, task-routed OSC 52 clipboard support, rolling-session PTY creation, transport-neutral state, paste-safe composer submission, an honest shared-dimensions notice, instance-resolved terminal canvases with ordered live settings propagation, and raw accessory submission that clears SwiftTerm's UIKit input state; added global image lightboxes and sandboxed interactive artifact viewers with authenticated sharing, task-isolated history, reconnect-safe loading, and strict cross-server data provenance; added reconnect-safe native web, attention, and terminal-bell notifications with settings-only authorization, foreground deduplication, stable replacement, distinct task badges, haptic controls, and validated cold-launch deep links; wired global agent-completion prompts with destructive native approval and reconnect-safe notification routing; added native read-only diff review with comparison modes, lazy syntax-highlighted unified patches, skipped-file details, and persistent read state; added live pull-request status details with CI checks, review threads, merge blockers, safe links, offline caching, and reconnect-safe refresh routing; added native task creation with a visibly required and accessible description, save, save-and-start, favorite and multi-variant agent selection, atomic Todo launch replacement, and preparation-gated terminal handoff, then wired it into connected-shell Work and project toolbars plus Todo primary and context Run actions with retry-safe preparation persistence; rebuilt terminal and Task Info state on same-server RPC replacement, rejected stale terminal input, resize, focus, and Task Info responses by connection lease, routed Todo starts through explicit agent and config launch, cleared modal routes before pairing, bound fresh-pair RPC ownership after authentication so the initial project and task refetch cannot be discarded, coalesced cold-start foreground events into the in-flight session bootstrap, made replaced or deleted sessions disconnect even after their SessionClient owner is released, reconciled stale task snapshots against the server during terminal handoff, surfaced canonical preparation failures, and honored explicit pending-task removals; treated manual pairing codes as one-time codes with platform-gated input behavior so iOS no longer offers to save them as passwords while macOS package tests keep compiling; kicked active PTY connections exactly once on scene foregrounding and network recovery without reacquiring the terminal session; routed completion requests through durable per-task socket ownership so agents reach the native client that owns their live PTY across concurrent app instances and remote restarts; validated native completion end to end with route dismissal plus tmux, claim, worktree, and branch cleanup; made CI run the app-target XCTest and XCUITest suites on a deterministic available iPhone Simulator; added an account-agnostic, unsigned-validatable TestFlight archive and local export workflow with explicit signing identity, version inputs, and signed Keychain-entitlement assertions; made Task Info changes and pull-request rows open the native Diff and PR Status destinations while preserving the originating task navigation stack; stabilized native Diff and PR destination stores across recomposition and removed stale task-review routes on task deletion pushes; and made slow terminal-move safety checks visible, serialized, bounded, and conservative when branch status is unavailable.
