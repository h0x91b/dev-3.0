Overhauled the built-in Claude preset list: added a pinned Sonnet 5 model block, added Medium/X-High reasoning-effort variants for Opus 4.8 and Sonnet 5 Auto/Bypass (replacing their plain-effort versions), dropped "Don't Ask" presets and the redundant generic "Sonnet" alias block, trimmed the Opus 4.7 fallback to Auto+Bypass, and moved Fable 5 to the top of every mode group. New default preset is Auto (Opus 4.8, X-High). Added the `xhigh` option to the Effort Level dropdown and `EffortLevel` type. Since preset *ordering* changes never used to reach already-onboarded installs (stored order is preserved forever), added a one-time `agentsLayoutRevision`-gated resync so existing users get the new order once too — see decision 096.
