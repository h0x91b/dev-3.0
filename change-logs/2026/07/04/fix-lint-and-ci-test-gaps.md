Fix two CI gate gaps found by a bug-hunt pass: `bun run lint` silently reported success when tsc failed to run at all (corrupt tsconfig, missing binary, crash) because it only surfaced diagnostics prefixed with `src/` and never checked the exit code — it now fails loudly on global/config tsc errors and on a nonzero exit without per-file diagnostics. Also wired the CLI test suite (vitest.config.cli.ts, ~470 tests, including the exit-code contract test) into `bun run test` and `bun run test:full`, which previously never ran it — so it now actually gates CI. Also made the getAccessUrl tests hermetic against an inherited DEV3_REMOTE_STATIC_CODE env var (they failed inside dev3-managed worktree shells).
