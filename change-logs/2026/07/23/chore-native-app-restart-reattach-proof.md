Added a focused app-restart reattach proof (seq 1247) for the isolated native-session registry: two separate short-lived controller processes prove that a live native terminal session survives full app-process turnover — controller A starts and marks a session then exits, and a clean controller B rediscovers and reattaches to the same host, shell, session, pane, and preserved shell state, with a deterministic single writer, an isolated stop, and honest lost-session results for stale/missing metadata. Test-only; changes no production behaviour and never touches the tmux path.
