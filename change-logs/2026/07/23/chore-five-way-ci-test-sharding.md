Split the full test workflow into five parallel CI shards, with each runner executing its mainview, bun, and CLI shard sequentially. This reduces wall-clock time while preventing the three Vitest suites from competing for one runner's worker pool, while still collecting every suite's result when one fails. Reconcile the tmux dependency audit with native-host and terminal-parity files added by concurrent merges.
