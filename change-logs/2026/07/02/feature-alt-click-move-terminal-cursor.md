Added iTerm2-style Alt/Option-click in the terminal to move the shell cursor: at a plain shell prompt, holding Option/Alt and clicking a cell walks the cursor there (the backend asks tmux what runs in the clicked pane, so TUIs like vim or htop are never touched). The terminal's mouse bridge now also forwards the Alt modifier, so Claude Code's own built-in Option-click works through dev3 too.
