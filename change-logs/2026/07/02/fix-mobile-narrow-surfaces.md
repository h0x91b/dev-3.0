Adapted two more surfaces to narrow (phone) viewports. The folder picker now replaces its fixed vertical Places sidebar and clipped filter column with a horizontal Places chip strip and stacked full-width inputs. The diff viewer now gives the diff full width on narrow, moves the file list into a bottom sheet behind a "Files" button, forces Unified view (Split is unusable at phone width), and no longer breaks file paths character-by-character.
