Extracted the Provider → Model → Mode launch picker into a shared AgentConfigPicker component and rolled it out to every surface that still used the old two-dropdown Agent + Configuration form: Spawn Agent, Spawn Bug Hunters, the global default-agent setting, and the per-column auto-launch agent. Also gave Fable 5 explicit Medium and X-High effort tiers for Auto and Bypass, mirroring Opus 4.8.
