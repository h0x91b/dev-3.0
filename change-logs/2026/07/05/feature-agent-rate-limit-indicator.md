Added an ambient agent rate-limit indicator to the global header: dev-3.0 now tracks Claude Code limit windows (5h/7d, % used + time-to-reset) by routing launched sessions' statusLine through a `dev3 statusline` wrapper (the user's own statusLine keeps working — dev3 delegates to it and appends a compact usage segment), and Codex windows/credits parsed from local session rollout files. The header gauge stays quiet below 80%, escalates to warning/danger colors at 80%/95%, and details live in its tooltip; a Global Settings → Behavior toggle disables the whole mechanism. No API calls — everything is parsed from local files.
