Added a bright animated "?" button to the global header — visible on every screen, it triggers help mode ("Explain this screen", same as ⇧⌘/). The hand-drawn icon redraws its question-mark curve and drops the dot in on hover, matching the other animated header glyphs. Escape now only exits help mode instead of also navigating back a screen, and the header's Project Terminal button label was shortened to "Terminal" to keep the row compact.
